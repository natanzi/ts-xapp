# Use the official Grafana image as the base
FROM grafana/grafana:latest

# Optional: Set environment variables if you have any specific to Grafana
# ENV GF_XXXX=YYYYY

# Expose the Grafana port (default is 3000)
EXPOSE 3000

# Set the entrypoint (this is usually defined in the base image, but can be overridden if necessary)
ENTRYPOINT ["/run.sh"]
